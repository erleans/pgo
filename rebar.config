{erl_opts, [debug_info]}.

{deps, [sbroker,
        backoff,
        jsx,
        {pgsql, {git, "https://github.com/semiocast/pgsql.git", {branch, "master"}}}]}.

{shell, [{apps, [pgo]},
         {config, "config/example.config"}]}.
